export default function AgentSetup() {
    return (
      <div style={{ maxWidth: 900, margin: "24px auto", padding: "0 16px" }}>
        <h2>Agent Setup</h2>
        <p>If you see this, routing + import works.</p>
      </div>
    );
  }